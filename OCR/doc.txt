1. Tải các thư viện cần thiết: 
 - pip install -r requirements.txt
2. Chạy sever:
 - uvicorn main:app --host 0.0.0.0 --port 8000 --reload
3. Truy cập vào: http://localhost:8000/docs để test api trên local
